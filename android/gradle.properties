org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

kotlin.jvm.target.validation.mode=warning

# AdMob application id (the "~" id from apps.admob.com). Uncomment and set your
# real id for release builds. If left unset the build uses Google's TEST app id,
# which is safe but earns nothing. AdMob ids are embedded in the shipped APK, so
# this is not a secret. See ADMOB_SETUP_GUIDE.md.
# admobAppId=ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY
